{
  "name": "autoprofit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "build": "node --check src/server.js && node --check src/auth.js && node --check src/db.js",
    "test": "node --test",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "migration:check": "node scripts/migration-check.js"
  },
  "dependencies": { "pg": "^8.16.3" }
}